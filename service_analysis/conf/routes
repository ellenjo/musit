# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET           /service/analysis/buildinfo                                controllers.Application.buildInfo

# StorageNode endpoints
# ~~~~
GET           /v1/museum/analysis/:eventId                              controllers.AnalysisEventController.getById(eventId: Long)